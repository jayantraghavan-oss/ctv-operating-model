Desktop view (1280px): Tree layout renders correctly with 4-column grid. All nodes visible.
TypeScript: No errors, LSP: No errors.
Tour overlay renders correctly.

Mobile changes made:
1. C5 nodes: 2 per row on mobile (flex-1 min-w-0) vs 4 per row desktop (w-[185px])
2. Cluster columns: MobileClusterStack accordion on mobile vs grid-cols-4 on desktop
3. MobileClusterStack: vertical accordion with expand/collapse per cluster, colored borders, active count indicators
4. Removed min-w-[900px] and overflow-x-auto on mobile
5. Smaller padding/spacing on mobile elements
6. Legend: flex-wrap on mobile
7. ScenarioPickerModal: max-h-[90vh] overflow-y-auto for scrolling on small screens
8. DemoNarrationBar: more compact padding/gaps on mobile
9. Footer: smaller text and margins on mobile
